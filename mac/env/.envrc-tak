export GITLAB_TOKEN=$GITLAB_TOKEN_TAK
export GITLAB_HOST=$GITLAB_HOST_TAK
export GITLAB_USER=$GITLAB_USER_TAK
export GITLAB_PROJECT_ID=$(gitlab-get-project-id-from-current-repo.sh)
export GITLAB_REGISTRY_ID=$(glab api projects/$GITLAB_PROJECT_ID/registry/repositories | jq ".[].id")

# gitlab-check-image-publish-time.sh $GITLAB_PROJECT_ID test-config

export JAVA_HOME=`/usr/libexec/java_home -v 17`; java --version
